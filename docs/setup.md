# Configuration du Projet (setup)
This guide explains how to install and run the project on your local machine.

# Prerequisites
Before getting started, make sure you have the following installed:
- Python 3.10 or higher
- Git

# Installation
1. Clone the repository

```
git clone https://github.com/RicoVorihery/orderpilot.git 
cd project-name
```
2. Install the dependencies
Create a virtual environment and install dependencies:
```
python -m venv .venv
# Windows
.venv\Scripts\activate
# Mac/Linux
source .venv/bin/activate
pip install -r requirements.txt
```

3. Copy the environment setting file
Copy the example environment file and fill in your credentials:
```
cp .env.example .env
```
# Run the project
> ⚠️ Work in progress — entrypoint will be updated as the project evolves.
